apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-be-to-db
  namespace: data-ns
spec:
  podSelector:
    matchLabels:
      app: bookstore-db
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          name: backend-ns
      podSelector:
        matchLabels:
          app: bookstore-be
    ports:
    - port: 3306
